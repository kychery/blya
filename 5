#задача5
def is_self_dividing(n):
    n = input()
    a = int(n)
    x=0
    for i in range(len(n)):
        if a % int(n[i]) !=0:
            x=x+1
    if x==0:
        print('True')
    else:
        print('False')
is_self_dividing(n)
